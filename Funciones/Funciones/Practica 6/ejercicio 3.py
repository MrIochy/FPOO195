#3. Escribir una función que convierta un número decimal en binario y otra que convierta un número binario en decimal.
pregunta = int(input("Digite 1 para conversion de decimal a binario o 2 para conversion de binario a decimal"))
if pregunta == 1:
    def binarioadecimal(numero):
        
elif pregunta ==2:
    
else:
    print("No has seleccionado ninguna opcion")